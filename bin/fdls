#!/usr/bin/env bash

# list PIDs with their non-txt-type file descriptors count
for pid in `ps aux | tail -n +2 | awk '{print $2}'`; do
  FCOUNT=`lsof -p $pid | grep -v " txt " | wc -l`; echo "PID: $pid $FCOUNT";
done | sort -nk3
